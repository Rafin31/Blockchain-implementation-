const k = 3;


function getCluster() {
    let cluster = [];
    for (let i = 0; i < k; i++) {
        cluster.push([])
    }

    const colors = ["red", "blue", "yellow", "green", "black", "white", "orange", "gray", "brown", "pink"]
}